services:
  oracle:
    extends:
      file: deps.yml
      service: oracle

  keycloak:
    extends:
      file: deps.yml
      service: keycloak

  puppet:
    extends:
      file: deps.yml
      service: puppet

  bam:
    hostname: bam
    container_name: bam
    ports:
      - "8443:8443"
      - "8080:8080"
      - "9990:9990"
    environment:
      KEYCLOAK_REALM: 'test-realm'
      KEYCLOAK_RESOURCE: 'bam'
      KEYCLOAK_SECRET: '9AubPamv1H9fAWo0q239EP7wPuH5ihb7'
      KEYCLOAK_SERVER: 'keycloak:8080'
      KEYCLOAK_WAR: 'bam.war'
      LOGBOOK_SERVER: 'elog'
      ORACLE_DATASOURCE: 'bam'
      ORACLE_SERVER: 'oracle:1521'
      ORACLE_USER: 'BAM_OWNER'
      ORACLE_PASS: 'password'
      ORACLE_SERVICE: 'xepdb1'
      PUPPET_SHOW_SERVER: 'puppet:3000'
      WILDFLY_USER: 'admin'
      WILDFLY_PASSW: 'admin'