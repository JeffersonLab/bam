---
version: '3.8'

services:
  oracle:
    image: gvenzl/oracle-xe:21.3.0
    hostname: oracle
    container_name: oracle
    ports:
      - 1521:1521
      - 5500:5500
    environment:
      ORACLE_PASSWORD: testing
    volumes:
      - ./docker/oradata:/opt/oracle/oradata
      - ./docker/orasetup:/container-entrypoint-initdb.d